name: Frontend CI/CD

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16" # Or your Node.js version

      - name: Install dependencies
        working-directory: ./frontend
        run: npm install

      - name: Run lint checks (Optional)
        working-directory: ./frontend
        run: npm run lint # Or your lint command

      - name: Run tests (Optional)
        working-directory: ./frontend
        run: npm test

      - name: Build Docker image
        run: |
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_TOKEN }} ghcr.io
          docker build -t ghcr.io/${{ github.repository }}/frontend:${{ github.sha }} ./frontend
          docker push ghcr.io/${{ github.repository }}/frontend:${{ github.sha }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server (Example - needs customization!)
        #  This step is highly dependent on your deployment environment.
        #  You'll likely need SSH access to your server.
        #  This is just a placeholder!  Replace with your actual deployment commands.
        run: |
          echo "Placeholder:  Deploying frontend..."
          #Example Deployment Steps
          #ssh your_user@your_server "
          #   cd /path/to/your/app
          #   docker-compose pull frontend
          #   docker-compose up -d frontend
          #   docker-compose up -d frontend
          #"
